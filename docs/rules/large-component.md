# Large Component

**ID**: `heavy-component`

## Description

Identifies components that are excessively large (default > 300 lines). Large components are harder to read, test, and maintain.

## Recommendation

Split the component into smaller, focused sub-components or extract logic into custom hooks.

## Auto-Fix

❌ Not supported automatically. Use the `refactor` command to help split it manually.
